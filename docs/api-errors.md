# API error codes

- `VALIDATION_FAILED` — request shape or domain validation failed.
- `ENTITY_NOT_FOUND` — no canonical entity exists for the supplied Wonder ID.
- `ROUTE_NOT_FOUND` — the route is not part of the published API contract.
- `INTERNAL_ERROR` — an unexpected failure occurred; implementation details are never exposed.
