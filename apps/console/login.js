const API_BASE=localStorage.getItem('wonderos_api_base')||'http://localhost:8080';
const form=document.querySelector('#login-form'); const errorBox=document.querySelector('#login-error');
form.addEventListener('submit',async event=>{event.preventDefault();errorBox.hidden=true;const data=Object.fromEntries(new FormData(form));try{const response=await fetch(`${API_BASE}/v1/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const payload=await response.json();if(!response.ok||!payload.success)throw new Error(payload.error?.message||'Sign in failed.');sessionStorage.setItem('wonderos_session_token',payload.data.token);sessionStorage.setItem('wonderos_user',JSON.stringify(payload.data.user));location.href='./lens-studio.html';}catch(error){errorBox.textContent=error.message;errorBox.hidden=false;}});
